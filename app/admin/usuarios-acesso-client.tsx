"use client";import {useState} from "react";type U={id:string;email:string;funcao:string;associacao:string|null;ativo:boolean};export default function UsuariosAcesso({usuarios}:{usuarios:U[]}){const[msg,setMsg]=useState("");async function alterar(u:U){const f=new FormData();f.set("id",u.id);f.set("ativo",String(!u.ativo));const r=await fetch("/api/admin/usuarios/status",{method:"POST",body:f});const b=await r.json();setMsg(b.message);if(r.ok)location.reload();}return <section className="mt-6 rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Acesso dos usuários</h2><p className="text-sm text-slate-600">Libere ou bloqueie o login por e-mail e senha.</p>{msg&&<p className="mt-2 text-sm font-bold">{msg}</p>}<div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b text-slate-500"><th className="py-2">E-mail</th><th>Perfil</th><th>Associação</th><th>Status</th><th></th></tr></thead><tbody>{usuarios.map(u=><tr key={u.id} className="border-b"><td className="py-3 font-semibold">{u.email}</td><td>{u.funcao}</td><td>{u.associacao||"-"}</td><td>{u.ativo?"Liberado":"Bloqueado"}</td><td className="text-right"><button onClick={()=>alterar(u)} disabled={u.funcao==="admin"} className={`rounded-lg px-3 py-2 font-bold text-white disabled:opacity-40 ${u.ativo?"bg-red-700":"bg-emerald-700"}`}>{u.ativo?"Bloquear":"Liberar"}</button></td></tr>)}</tbody></table></div></section>}
