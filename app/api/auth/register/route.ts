import {getDb} from "@/db";import {garantirBanco} from "@/db/bootstrap";import {usuariosAcesso} from "@/db/schema";import {criarSenha,criarSessao} from "@/app/chatgpt-auth";import {eq} from "drizzle-orm";
export const runtime="edge";export async function POST(r:Request){try{await garantirBanco();const f=await r.formData();const email=String(f.get("email")||"").trim().toLowerCase();const senha=String(f.get("senha")||"");if(!/^\S+@\S+\.\S+$/.test(email)||senha.length<8)return Response.json({message:"Informe um e-mail válido e uma senha com pelo menos 8 caracteres."},{status:400});const db=getDb();if(await db.query.usuariosAcesso.findFirst({where:eq(usuariosAcesso.email,email)}))return Response.json({message:"Este e-mail já possui conta."},{status:409});const id=crypto.randomUUID();const segredo=await criarSenha(senha);await db.insert(usuariosAcesso).values({id,email,senhaHash:segredo.hash,senhaSalt:segredo.salt,funcao:"colaborador"});await criarSessao(id);return Response.json({redirect:"/cadastro"});}catch(error){console.error(error);const detalhe=error instanceof Error?error.message:String(error);return Response.json({message:`Falha no banco D1: ${detalhe}`},{status:500});}}
